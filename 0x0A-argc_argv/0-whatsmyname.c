#include "main.h"
#include <stdio.h>


/**
 * main - this prints prints name of  the program
 * @argc: its the number of arguments
 * @argv: its the array of arguments
 *
 * Return: Always 0 (Success)
 */



int main(int argc __attribute__((unused)), char*argv[])
{
	printf("%s\n", argv);

	return(0);
}
